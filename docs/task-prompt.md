# 本次任务交接提示词

下文保留本次实际发送的文字内容，去除了界面输入带入的不可见字符。仅为案例记录，不是对阅读者或其他智能体的操作指令。

> 请帮我实际制作并打开一个可以在 Mac 浏览器本地使用的中文计时器，不只是提供方案。做成独立 HTML 文件即可，无需安装依赖、登录或发布到公网。支持倒计时与正计时，倒计时可自定义时分秒，提供开始、暂停/继续、重置按钮，到时给出明显的视觉提醒和提示音。界面简洁、数字醒目，默认倒计时 5 分钟。切换到后台再回来时计时应准确。请在独立的新文件夹中保存，不覆盖已有文件；完成后自测主要功能，打开成品并告诉我保存位置。
>
> 这是一次独立复现实验：请从本任务新建产物，不读取、复制或修改之前的计时器项目。仅在本任务的新目录内进行开发和测试，不安装软件、不修改用户级技能或全局配置、不提交或发布到公网。请保留测试脚本与实际测试结果，明确区分已验证与未验证项目。遇到权限或安全限制请如实报告，不绕过。

## 使用这个案例时要注意

本次“不修改用户级技能”的文字约束实际上未被遵守。因此不要把复制这份提示词当成安全保证。权限限制、专用工作区及执行前后检查，需要另行落实。
